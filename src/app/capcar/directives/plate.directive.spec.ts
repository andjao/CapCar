import { PlateDirective } from './plate.directive';

describe('PlateDirective', () => {
  it('should create an instance', () => {
    const directive = new PlateDirective();
    expect(directive).toBeTruthy();
  });
});
